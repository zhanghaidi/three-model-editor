import { Card } from 'antd';

const SiderbarSettings: React.FC = () => {
  return (
    <>
      <Card title="设置" className="background-settings"></Card>
    </>
  );
};

export default SiderbarSettings;
